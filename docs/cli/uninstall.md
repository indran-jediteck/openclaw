---
summary: "CLI reference for `lynkai uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
title: "uninstall"
---

# `lynkai uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
lynkai uninstall
lynkai uninstall --all --yes
lynkai uninstall --dry-run
```
